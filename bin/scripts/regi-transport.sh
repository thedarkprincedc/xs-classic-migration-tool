regi transport ${REGI_DELIVERY_UNIT} \
    --sourceHostname=${REGI_SOURCE_HOSTNAME} \
    --sourceUser=${REGI_SOURCE_USERNAME} \
    --sourcePassWord=${REGI_SOURCE_PASSWORD} \
    --targetHostName=${REGI_TARGET_HOSTNAME} \
    --targetUserName=${REGI_TARGET_USERNAME} \
    --targetPassWord=${REGI_TARGET_PASSWORD}
